{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/test/local_test.js",
            "cwd": "${workspaceFolder}/test"
        }
    ]
}